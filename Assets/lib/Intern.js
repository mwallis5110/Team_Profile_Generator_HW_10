const Intern = require("./Employee");

//Creates Intern which extends Employee

class Intern extends Employee {
    constructor(employeeName, id, email) {
    super(school)
    this.school = school;
    }

    getSchool() {
        return this.school;
    };
};

module.exports = Intern
